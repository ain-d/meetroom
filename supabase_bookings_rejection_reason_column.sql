-- ============================================================================
-- เพิ่มคอลัมน์เก็บ "เหตุผลที่ปฏิเสธการจอง" — ใช้คู่กับ BookingAdmin.jsx เวอร์ชันใหม่
-- ที่บังคับให้แอดมินกรอกเหตุผลก่อนกดปฏิเสธได้ (เดิมมีแค่ window.confirm ยืนยัน ไม่มีที่เก็บเหตุผล)
--
-- วิธีติดตั้ง: เปิด Supabase Dashboard -> SQL Editor -> วางไฟล์นี้ทั้งหมด -> Run
-- ทำครั้งเดียว (รันซ้ำได้ปลอดภัย เพราะใช้ ADD COLUMN IF NOT EXISTS)
-- ============================================================================

ALTER TABLE public.bookings ADD COLUMN IF NOT EXISTS rejection_reason text;
